# Bloquea el acceso web directo a los archivos de sesión.
# Compatible con Apache 2.2 y 2.4 (cPanel puede correr cualquiera de los dos).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
